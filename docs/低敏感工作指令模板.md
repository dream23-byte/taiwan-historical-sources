# 低敏感工作指令模板（新 Session 開工用）

> 用途：避免對話累積高風險內容，導致上游供應商內容審查（Content Exists Risk）擋掉請求。
> 用法：開新 session 後，貼上下方指令（可自行勾選工作範圍）。

---

## 開工指令

請先讀取以下檔案後接續工作：
- `G:\我的雲端硬碟\各歷史資料庫\handoff.md`（交接狀態）
- `G:\我的雲端硬碟\各歷史資料庫\docs\校對清單_臺灣事件MVP.md`（待裁示事項）
- `G:\我的雲端硬碟\各歷史資料庫\docs\低敏感工作指令模板.md`（本檔）

**工作規範：**
1. 對話中不描述高風險主題的細節；涉及近代事件的資料操作，直接改資料庫／檔案，不在對話中複述內容原文。
2. 一切內容以檔案與資料庫為準（不把敏感細節帶進對話，就不會累積到審查閾值）。
3. 向使用者說明進度時，用「事件編號＋操作結果」呈現（例如「id 5 已修正」），不引用事件描述原文。
4. 每完成一個階段，更新 `handoff.md`。

**本次工作範圍（勾選）：**
- [ ] 修正已入庫事件（依校對清單第三節裁示）
- [ ] 搬入其餘軌事件（來源：本地暫存檔 `C:\Users\myaly\AppData\Local\Temp\opencode\code_artifact.html`，行 442–540）
- [ ] 建立事件與史料關聯（`event_sources`）
- [ ] 其他：＿＿＿＿＿＿

**技術備忘（沿用既有慣例）：**
- 事件表 id 為 identity，INSERT 需 `OVERRIDING SYSTEM VALUE`
- SQL 分批執行；驗證用計數查詢即可
- Edge Function 既有：`search-sources`、`event-detail`（勿重複建）
